import PlaceholderPanel from '../../components/PlaceholderPanel';

export default function ProductsPage() {
  return (
    <PlaceholderPanel
      title="Produtos (Descoberta)"
      phase="Fase 2"
      description="Vai listar produtos puxados automaticamente das redes de afiliados (Digistore24, ClickBank, CJ, Impact, Awin, PartnerStack), com comissão, EPC, taxa de conversão e países permitidos."
    />
  );
}
